import React from 'react';

const BorrowedBookCard = ({book}) => {
    console.log(book)
    return (
        <div>
            i ma borrowd book card
        </div>
    );
};

export default BorrowedBookCard;